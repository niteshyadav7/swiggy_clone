let arr = [2, 1, 5, 3, 7, 6];

// sort array in increasing order::

const newArr = arr.sort((a, b) => a - b);
console.log(newArr);

// sort array in decreasing order::

const nextArr = arr.sort((a, b) => b - a);
console.log(nextArr);
